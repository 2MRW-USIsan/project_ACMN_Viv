"use client";

import { PostingClerkContexts } from "@/hooks/posting-clerk/state/usePostingClerkContext";

export function usePostingClerkInitialize(
  _contexts: PostingClerkContexts,
): void {
  // 初期化処理は後続工程で追加予定
}
